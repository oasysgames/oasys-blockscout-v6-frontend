{
  "name": "oasys-bridge-subgraph",
  "version": "0.1.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 oasys/bridge",
    "create-local": "graph create --node http://localhost:8020/ oasys/bridge",
    "remove-local": "graph remove --node http://localhost:8020/ oasys/bridge",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 oasys/bridge"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.51.0",
    "@graphprotocol/graph-ts": "0.31.0"
  }
} 